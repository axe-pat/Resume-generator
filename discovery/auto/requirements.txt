# job_pipeline dependencies
# Install with: pip install -r job_pipeline/requirements.txt

anthropic>=0.40.0
python-jobspy>=1.1.0
pandas>=2.0.0
openpyxl>=3.1.0
httpx[socks]>=0.27.0   # required by anthropic in some proxy environments
